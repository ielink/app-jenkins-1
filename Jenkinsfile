pipeline {
   agent any

   stages {
      stage('Build') {
               steps {
                  echo "$GIT_BRANCH"
               }
            }
      stage('rest') {
               steps {
                  echo "$GIT_BRANCH"
               }
            }
   }
}
